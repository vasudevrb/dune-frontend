The frontend web-app for Dune Imperium: Uprising board game. Built using React + TS + Vite.

To run, first install the required dependencies using `npm install` and then `npm run dev`. The page is hosted at the default vite `5173` port.